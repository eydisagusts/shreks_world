import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['ironusa.vtexassets.com', 
      'static.miraheze.org',
      'static.wikia.nocookie.net',
      'i.pinimg.com',
      'www.nj.com',
      'i2-prod.mirror.co.uk',
      'news.airbnb.com',
  ],
  },
  /* config options here */
};

export default nextConfig;
